{
    "/api": {
        "target": "http://localhost:4000",
        "secure": false,
        "changeOrigin": true,
        "timeout": 30000,
        "proxyTimeout": 30000,
        "onError": "function(err, req, res) { console.error('Proxy error:', err); res.writeHead(503, { 'Content-Type': 'application/json' }); res.end(JSON.stringify({ error: 'API server is unavailable' })); }"
    }
}